mod geometry;
mod scene_items;
mod scene_setup;
use scene_items::color::Color;
use scene_setup::scene::Scene;
fn main() {
    let tmp = Color{r: 0.0, g: 0.0, b: 0.0};
    println!("Hello, world!");
}
